div.form-content.form-sort-video
      
    form(method='POST' action='')
        div.form-group
            label(for='video_sort') Sort Videos By...
            select(name='video_sort' class='video-sort_dropdown')
                option(value="['author_name', 'descending']") Creator Username (A - Z)
                option(value="['author_name', 'ascending']") Creator Username (Z - A)
                option(value="['date', 'descending']") Date Added (new - old)
                option(value="['date', 'ascending']") Date Added (old - new)
                option(value="['title', 'descending']") Video Description (A - Z)
                option(value="['title', 'ascending']") Video Description (Z - A)
            button(type='submit', class='form-button') Sort Videos
        if errors
            ul
            for error in errors
                li!= error.msg